---
paths:
  - "config/**"
  - ".env*"
---

# セキュリティクリティカルファイル

- `credentials.ts` は `process.env` 経由のみ。ハードコード厳禁
- dotenvx で暗号化された `.env` はコミット対象（暗号文のため安全）
- `.env.keys` は絶対にコミット・表示・ログしない
- 復号済みの環境変数値をコード内に残さない
- テストデータにプロダクションのシークレットを使用しない
